# Auto-generated message functions

def msg_access_denied_content_rating() -> tuple[str, dict[str, str]]:
    return "msgs.access_denied_content_rating", {}

def msg_action_cancelled_duplicate_task() -> tuple[str, dict[str, str]]:
    return "msgs.action_cancelled_duplicate_task", {}

def msg_action_cancelled_folder_not_empty() -> tuple[str, dict[str, str]]:
    return "msgs.action_cancelled_folder_not_empty", {}

def msg_action_cancelled_wrong() -> tuple[str, dict[str, str]]:
    return "msgs.action_cancelled_wrong", {}

def msg_action_failed() -> tuple[str, dict[str, str]]:
    return "msgs.action_failed", {}

def msg_action_failed_missing() -> tuple[str, dict[str, str]]:
    return "msgs.action_failed_missing", {}

def msg_are_sure_delete_book() -> tuple[str, dict[str, str]]:
    return "msgs.are_sure_delete_book", {}

def msg_are_sure_delete_file(name) -> tuple[str, dict[str, str]]:
    return 'msgs.are_sure_delete_file', {"name": name}

def msg_are_sure_delete_files(count) -> tuple[str, dict[str, str]]:
    return 'msgs.are_sure_delete_files', {"count": count}

def msg_are_sure_delete_group() -> tuple[str, dict[str, str]]:
    return "msgs.are_sure_delete_group", {}

def msg_are_sure_delete_user() -> tuple[str, dict[str, str]]:
    return "msgs.are_sure_delete_user", {}

def msg_are_sure_migrate_file(name) -> tuple[str, dict[str, str]]:
    return 'msgs.are_sure_migrate_file', {"name": name}

def msg_are_sure_migrate_files(count) -> tuple[str, dict[str, str]]:
    return 'msgs.are_sure_migrate_files', {"count": count}

def msg_are_sure_move_files(count) -> tuple[str, dict[str, str]]:
    return 'msgs.are_sure_move_files', {"count": count}

def msg_are_sure_remove_pin() -> tuple[str, dict[str, str]]:
    return "msgs.are_sure_remove_pin", {}

def msg_are_sure_server_restart() -> tuple[str, dict[str, str]]:
    return "msgs.are_sure_server_restart", {}

def msg_are_sure_server_stop() -> tuple[str, dict[str, str]]:
    return "msgs.are_sure_server_stop", {}

def msg_are_sure_watched_files(count) -> tuple[str, dict[str, str]]:
    return 'msgs.are_sure_watched_files', {"count": count}

def msg_auth_feature_required() -> tuple[str, dict[str, str]]:
    return "msgs.auth_feature_required", {}

def msg_auth_issue_reason(reason) -> tuple[str, dict[str, str]]:
    return 'msgs.auth_issue_reason', {"reason": reason}

def msg_auth_login_failure() -> tuple[str, dict[str, str]]:
    return "msgs.auth_login_failure", {}

def msg_book_added() -> tuple[str, dict[str, str]]:
    return "msgs.book_added", {}

def msg_book_removed() -> tuple[str, dict[str, str]]:
    return "msgs.book_removed", {}

def msg_end() -> tuple[str, dict[str, str]]:
    return "msgs.end", {}

def msg_file_deleted() -> tuple[str, dict[str, str]]:
    return "msgs.file_deleted", {}

def msg_file_migrated() -> tuple[str, dict[str, str]]:
    return "msgs.file_migrated", {}

def msg_file_moved() -> tuple[str, dict[str, str]]:
    return "msgs.file_moved", {}

def msg_file_updated() -> tuple[str, dict[str, str]]:
    return "msgs.file_updated", {}

def msg_file_uploaded() -> tuple[str, dict[str, str]]:
    return "msgs.file_uploaded", {}

def msg_folder_created() -> tuple[str, dict[str, str]]:
    return "msgs.folder_created", {}

def msg_folder_deleted() -> tuple[str, dict[str, str]]:
    return "msgs.folder_deleted", {}

def msg_folder_moved() -> tuple[str, dict[str, str]]:
    return "msgs.folder_moved", {}

def msg_folder_updated() -> tuple[str, dict[str, str]]:
    return "msgs.folder_updated", {}

def msg_found_x_results(count) -> tuple[str, dict[str, str]]:
    return 'msgs.found_x_results', {"count": count}

def msg_found_x_results_removed_y(count, removed) -> tuple[str, dict[str, str]]:
    return 'msgs.found_x_results_removed_y', {"count": count, "removed": removed}

def msg_info_archiving_file(fileName, index, total) -> tuple[str, dict[str, str]]:
    return 'msgs.info_archiving_file', {"fileName": fileName, "index": index, "total": total}

def msg_info_deleting_file(fileName, index, total) -> tuple[str, dict[str, str]]:
    return 'msgs.info_deleting_file', {"fileName": fileName, "index": index, "total": total}

def msg_info_moving_file(fileName, index, total) -> tuple[str, dict[str, str]]:
    return 'msgs.info_moving_file', {"fileName": fileName, "index": index, "total": total}

def msg_info_uploading_file(fileName, index, total) -> tuple[str, dict[str, str]]:
    return 'msgs.info_uploading_file', {"fileName": fileName, "index": index, "total": total}

def msg_invalid_parameter(name) -> tuple[str, dict[str, str]]:
    return 'msgs.invalid_parameter', {"name": name}

def msg_mismatched_parameters(p1, p2) -> tuple[str, dict[str, str]]:
    return 'msgs.mismatched_parameters', {"p1": p1, "p2": p2}

def msg_missing_parameter(name) -> tuple[str, dict[str, str]]:
    return 'msgs.missing_parameter', {"name": name}

def msg_no_operation() -> tuple[str, dict[str, str]]:
    return "msgs.no_operation", {}

def msg_operation_complete() -> tuple[str, dict[str, str]]:
    return "msgs.operation_complete", {}

def msg_operation_failed() -> tuple[str, dict[str, str]]:
    return "msgs.operation_failed", {}

def msg_operation_failed_for(name) -> tuple[str, dict[str, str]]:
    return 'msgs.operation_failed_for', {"name": name}

def msg_preview_updated() -> tuple[str, dict[str, str]]:
    return "msgs.preview_updated", {}

def msg_removed_x_items(count) -> tuple[str, dict[str, str]]:
    return 'msgs.removed_x_items', {"count": count}

def msg_server_error() -> tuple[str, dict[str, str]]:
    return "msgs.server_error", {}

def msg_task_added() -> tuple[str, dict[str, str]]:
    return "msgs.task_added", {}

def msg_task_cancelled() -> tuple[str, dict[str, str]]:
    return "msgs.task_cancelled", {}

def msg_task_updated() -> tuple[str, dict[str, str]]:
    return "msgs.task_updated", {}

def msg_tasks_started(started, skipped) -> tuple[str, dict[str, str]]:
    return 'msgs.tasks_started', {"started": started, "skipped": skipped}
